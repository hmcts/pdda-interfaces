java:
  # Don't modify below here
  image: ${IMAGE_NAME}
  ingressHost: ${SERVICE_FQDN}
  environment:
    RUN_DB_MIGRATION_ON_STARTUP: true
    POSTGRES_USER: "{{ .Values.postgresql.auth.username }}"
  testsConfig:
    environment:
      TEST_URL: http://pdda-public-display-data-aggregator-staging.staging.platform.hmcts.net